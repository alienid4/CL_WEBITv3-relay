# AGENTS.md — 給協作 AI（公司 Codex）的規則

> **你的角色：寫測試。程式碼不歸你改。**
>
> 這份是硬規則，不是建議。跟你自己的判斷衝突時，以這份為準；覺得這份有問題，
> 寫進交接文件的「我不確定的」欄，不要自己改做法。

---

## 0. 一句話說明這個專案

金融業的 IT 資產盤點系統。後端 FastAPI + SQLite，前端 Nuxt 3。
它產出的數字會被拿去做決策、會被資安稽核看。所以**「數字看起來對」不夠，
要「數字錯的時候看得出來」**。

---

## 1. 你的邊界

| 可以動 | 不可以動 |
|---|---|
| `tests/asset_module/**`（後端測試） | `APP/asset-module/backend/**` |
| `APP/asset-module/frontend/tests/**`（前端測試） | `APP/asset-module/frontend/`（除 `tests/` 外） |
| — | `.project/**`、`schema.sql`、任何設定檔 |

**發現程式有 bug 時：寫一條會紅的測試 + 在交接文件裡說明，不要自己去改程式。**
那條紅的測試就是最好的 bug 報告——它可以被重現，而文字描述不行。

---

## 2. ⚠️ 你的產出會 email 出公司，不得含真實資料

這是**法遵要求**，不是風格偏好。

**禁止出現在你任何產出裡**：
- 真實 IP（公司內網段）
- 真實主機名（實際存在的機器）
- 公司名稱、子公司名稱、系統代號
- 人員姓名、分機、email
- 資產序號

**一律改用**：
- IP → `10.99.x.x`（這個段是專門留給範例的）
- 主機名 → `SECSVR001-001`、`host-a`、`vm-a`
- 分類名稱 → 用測試自己建的假分類，不要抄真實清單
- 人名 → `tester`、`使用者A`

**你拿到的程式碼已經去識別化過**，裡面看到的 IP 與名稱本來就是假的——照抄那些就對了。
如果你在程式碼裡看到看起來像真的東西，那是漏網，**回報但不要複製**。

---

## 2b. 連正式主機：**唯讀，而且要明講唯讀**

你可能拿得到公司正式主機的存取權（例如查真實網段來做防火牆申請）。
那是合理的用途——那些資料只有那裡有。但有三條不能破：

### ① 不准改動那台的任何東西

不改檔案、不重啟服務、不呼叫任何會寫入的 API（POST/PUT/DELETE）、不裝套件。
那台是**用 patch 部署的**，沒有 git、沒有版控——你在那裡改的東西
**下一次套 patch 就被蓋掉，而且沒有任何紀錄說明它曾經存在**。

要改程式，走正常流程：你寫測試 → 交接文件 → 由程式端改。

### ② 讀 SQLite 一定要用唯讀模式

Python 的 `sqlite3.connect(path)` **預設是讀寫**，而且會在旁邊建
`-wal` / `-shm` 檔。正式服務正在跑，後果是：

  · 那兩個檔被你的帳號建立 → 服務那邊在**很後面**才噴權限錯誤，
    症狀看起來跟你完全無關（這個專案已經踩過三次同類問題）
  · 拿到寫鎖 → 服務出現 `database is locked`

**正確寫法**：

```python
conn = sqlite3.connect("file:/path/to/asset.db?mode=ro", uri=True)
```

更保險的做法是先複製一份到自己的暫存目錄再讀。查詢一律 SELECT，
不要用 `PRAGMA` 改設定。

### ③ 真實資料**不可以進交接文件**

你在那台看到的真實 IP、主機名、人員姓名——**留在公司內部**。
交給我的那份 Markdown 會 email 出公司（見第 2 節）。

需要用真實資料產出的東西（例如防火牆申請表）**留在公司自己用**，
不要放進交接文件。要在交接文件裡描述時，改用 `10.99.x` 這種範例值。

### 哪台有什麼，不一樣

| | 正式主機 | 你拿到的程式碼包（relay） |
|---|---|---|
| 後端程式碼 | 有 | 有 |
| **前端原始碼** | **沒有**（只有 build 過的產物） | 有 |
| **`tests/`** | **沒有** | 有 |
| `AGENTS.md`、`.project/` | 沒有 | 有 |
| **真實資料** | **只有這裡有** | 沒有 |

所以：**看程式碼用 relay**（比較完整），**看資料才連正式機**。
在正式機上讀程式碼會少看到一半，還可能看到過期的版本。

## 3. 測試怎麼寫（比「寫了幾個」重要得多）

### 3.1 測試名稱講「守什麼」，不是講「測什麼函式」

```python
# ✗ 不要
def test_classify_rows():

# ✓ 要
def test_逐台設定會壓過api_id對照表推出來的預設():
def test_超過視窗就不算在線_session還沒過期也一樣():
```

理由：三個月後有人看到這條測試紅了，要能**立刻知道壞掉的是哪個保證**，
而不是去讀 assert 猜意圖。

### 3.2 每個重要的 assert 旁邊寫「為什麼這條重要」

尤其是踩過的坑。看 `tests/asset_module/test_activity.py` 與
`APP/asset-module/frontend/tests/useSort.spec.ts` 的寫法。

### 3.3 測「錯的時候會怎樣」，不是只測快樂路徑

這個專案最貴的錯誤全部長同一個樣子：**不會報錯，但數字悄悄變了**。
所以優先測：

- 邊界（0 筆、1 筆、空字串、None）
- 「查不到」與「值是空的」要**分得出來**——這在這個專案是兩件事
- 失敗路徑（權限不足、格式錯、重複匯入）
- **冪等**（同樣的操作跑兩次，結果要一樣）

### 3.4 兩個方向都要釘

條件寫反了，只釘一個方向的測試照樣會過。

```python
# 只釘這個不夠
assert 環境是測試的機器_算進測試桶()
# 還要釘反方向
assert 環境是正式但分類是測試的機器_也算進測試桶()
```

### 3.5 ⚠️ 沒看過它紅過的測試，不算測試

**寫完每一條，自問：「程式壞成什麼樣，這條會紅？」**
如果答不出來，那條測試是裝飾品——比沒有更糟，因為它給人「這裡有防護」的錯覺。

跑得動測試的話，實際做一次：把對應的程式改壞，確認那條真的會紅，再改回來。
跑不動的話，把「我預期它在什麼情況下會紅」寫進交接文件。

真實案例（2026-08-26，寫這份規則的人自己踩的）：一條「IP 要按數值排」的測試，
把程式裡專門處理 IP 的分支**整個拿掉，測試照樣是綠的**——因為另一條路
（`localeCompare` 的 `numeric:true`）剛好也排得對。那條測試守的是行為，
不是那段實作，但作者原本以為它在保護那段程式碼。

---

## 4. 前端測試怎麼跑

```bash
cd APP/asset-module/frontend
npm test              # vitest run
```

環境：`vitest` + `@vue/test-utils` + `happy-dom`。設定見 `vitest.config.ts`。
測試放 `frontend/tests/*.spec.ts`。

⚠️ **沒有用 `@nuxt/test-utils`**，所以用到 Nuxt 自動匯入（`useApi`／`useToast`／
`definePageMeta`）的**頁面元件沒辦法直接掛載**。

要測那種頁面的邏輯：**在交接文件裡提出「這段建議抽成 composable」**，
由程式端（不是你）去抽，抽完你再測。不要為了測而在測試裡 mock 一堆 Nuxt 內部。

**優先測的東西**（照這個順序）：
1. `composables/` 底下的純邏輯
2. 元件的**條件顯示**：什麼情況顯示警告、什麼情況顯示空狀態
3. 「0」與「沒有資料」在畫面上要**分得出來**

## 5. 後端測試怎麼跑

```bash
python -m pytest tests/asset_module -q          # 全部，約 6 分鐘
python -m pytest tests/asset_module/test_xxx.py -q
```

跑不動就跑不動，**照實說**（見第 6 節）。不要因為跑不了就把測試寫得「一定會過」。

---

## 6. 你怎麼交東西給我

**你產出一份 Markdown 文件，由使用者 email 出來。格式見 `AI/CODEX_HANDOFF_TEMPLATE.md`，
照那個格式寫，不要自由發揮。**

三條硬規則：

1. **一節一個檔案，給完整路徑與完整內容。**
   不准用 `...`、`（其餘不變）`、diff 片段。我要能機械式地把它還原成檔案。

2. **報事實，不下結論。**
   「哪條測試、完整輸出、怎麼重現」是事實。「我認為是 X 造成的」是推論——
   可以寫，但**必須標明是推論**，跟事實分開。

   理由：你看不到全貌。一個標成事實的錯誤推論，會讓我往錯的方向查，
   那比沒有分析更花時間。

3. **每條測試標「預期」**：`預期：綠` 或 `預期：不確定`。

   你不一定跑得了測試，所以我一律當**未驗證**。有這個標籤我才分得出
   「你發現了一個 bug」跟「你把測試寫壞了」——`預期：綠` 的東西跑出來紅，
   那是需要查的訊號；`預期：不確定` 的紅了，那只是草稿。

---

## 7. 這個專案的幾條底線（你也適用）

- **「查不到」跟「沒有」是兩件事**，畫面上與測試裡都要分得出來
- **加總與下鑽必須走同一份計算**——格子上的數字跟點進去的筆數不一致，報表就廢了
- **不要猜**。看不懂的資料寧可留白並標示，不要填一個看起來合理的值
- **幾分證據說幾分話**：跑過的是證據，推出來的是推論，沒跑的是未驗證，三者要分開講
